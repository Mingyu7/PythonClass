from tkinter import *

root = Tk()
root.title("Nado GUI")
root.geometry("600x480") #가로 * 세로
# root.geometry("600x480+300+100") 가로 * 세로 + X좌표 + Y좌표

root.resizable(True,False) # x(너비), y(높이) 값 변경 X (창 크기 변경불가)

root.mainloop()